version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: statement_db
      POSTGRES_USER: statement_user
      POSTGRES_PASSWORD: statement_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: